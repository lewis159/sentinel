; react-grid-layout@1.x declares a peer dep on react <19 but runs fine on
; react@19 (it only uses stable APIs). Allow the install to resolve rather than
; hard-fail on the peer mismatch. Applies to local installs and the Docker build.
legacy-peer-deps=true
